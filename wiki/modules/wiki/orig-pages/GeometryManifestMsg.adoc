Sent after a geometry req is recv'd, but before the chunks are sent.

uint32 number of objects/strings/chunks.

List of GSStrings for returned objects.

ReUUID is the same as the GeometryReqMsg's UUID.
